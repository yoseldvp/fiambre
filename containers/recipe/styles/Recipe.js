import { StyleSheet } from "react-native";

import Colors from "../../../common/Colors";

const styles = StyleSheet.create({
  base: {
    backgroundColor: Colors.bakground,
    flex: 1,
  }
});

export default styles;